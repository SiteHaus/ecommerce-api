meta {
  name: List Public Collections
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/v1/catalog/collections
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });

  test("returns array of collections", function() {
    expect(res.body).to.be.an("array");
  });
}
