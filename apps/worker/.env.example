NODE_ENV=development

DATABASE_URL=postgresql://ecom:ecom@localhost:5434/ecommerce
DB_POOL_SIZE=5

# Redis — BullMQ queue backend (same instance as gateway throttler)
REDIS_URL=redis://localhost:6380

# Resend
RESEND_API_KEY=
EMAIL_FROM=
